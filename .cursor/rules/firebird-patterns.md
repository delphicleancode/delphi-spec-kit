---
description: "Padrões Firebird Database — conexão FireDAC, PSQL, generators, transactions, queries"
globs: ["**/*.pas", "**/*.sql"]
alwaysApply: false
---

# Firebird Database — Cursor Rules

Use estas regras ao desenvolver com banco de dados Firebird via FireDAC.

## Conexão Obrigatória

```pascal
{ SEMPRE configurar: }
FConnection.DriverName := 'FB';
FConnection.Params.Values['CharacterSet'] := 'UTF8';
FConnection.Params.Values['SQLDialect'] := '3';  { NUNCA Dialect 1 }
```

## Generators e Auto-Increment

| Abordagem | Versão | Exemplo |
|-----------|--------|---------|
| `GEN_ID(generator, 1)` | Todas | Clássico com trigger BI |
| `NEXT VALUE FOR seq` | FB 3+ | Sintaxe SQL padrão |
| `IDENTITY` | FB 3+ | `GENERATED BY DEFAULT AS IDENTITY` |
| `RETURNING id` | Todas | Retorna ID após INSERT |

## RETURNING — Regra Crítica

```pascal
// ❌ ExecSQL perde o valor retornado!
LQuery.SQL.Text := 'INSERT INTO ... RETURNING id';
LQuery.ExecSQL;

// ✅ Open para receber RETURNING
LQuery.SQL.Text := 'INSERT INTO ... RETURNING id';
LQuery.Open;
LId := LQuery.Fields[0].AsInteger;
```

## Stored Procedures

| Tipo | SUSPEND | Chamada Delphi |
|------|---------|----------------|
| **Selectable** (retorna dados) | Sim | `SELECT * FROM SP_NOME(...)` + `Open` |
| **Executable** (ação) | Não | `EXECUTE PROCEDURE SP_NOME(...)` + `ExecSQL` |

## Transactions

```pascal
{ Transação explícita para operações compostas }
FConnection.StartTransaction;
try
  FRepoA.Insert(LObjA);
  FRepoB.Insert(LObjB);
  FConnection.Commit;
except
  FConnection.Rollback;
  raise;
end;
```

| Isolation | FireDAC | Uso |
|-----------|---------|-----|
| Read Committed | `xiReadCommitted` | ✅ Padrão CRUD |
| Snapshot | `xiSnapshot` | Relatórios consistentes |
| Serializable | `xiSerializable` | Lock exclusivo (raro) |

## Tratamento de Erros Firebird

```pascal
except
  on E: EFDDBEngineException do
  begin
    case E.Kind of
      ekRecordLocked:
        raise EConflictException.Create('Registro bloqueado');
      ekUKViolated:
        raise EDuplicateException.Create('Registro duplicado: ' + E.Message);
      ekFKViolated:
        raise EDependencyException.Create('Violação de FK: ' + E.Message);
    else
      raise;
    end;
  end;
end;
```

## Tipos de Dados — Cuidados

| Firebird | Cuidado |
|----------|---------|
| `DATE` (Dialect 3) | Apenas data, sem hora — use `TIMESTAMP` para data+hora |
| `CHAR(N)` | Preenche com espaços — prefira `VARCHAR(N)` |
| `BLOB SUB_TYPE TEXT` | CLOB — usar `AsMemo` / `AsString` |
| `NUMERIC(P,S)` | Exato — usar para valores monetários |
| `BOOLEAN` | Apenas FB 3+ — usar `SMALLINT CHECK (VALUE IN (0,1))` para FB 2.5 |

## Domains — Tipos Reutilizáveis

```sql
CREATE DOMAIN DM_ID     AS INTEGER NOT NULL;
CREATE DOMAIN DM_NAME   AS VARCHAR(100) NOT NULL;
CREATE DOMAIN DM_MONEY  AS NUMERIC(15,2) DEFAULT 0;
CREATE DOMAIN DM_STATUS AS SMALLINT DEFAULT 0 CHECK (VALUE BETWEEN 0 AND 2);
```

## Proibições em Firebird

- ❌ Dialect 1 — usar SEMPRE Dialect 3
- ❌ Concatenar SQL — usar parâmetros parametrizados
- ❌ Ignorar `CharacterSet` — definir `UTF8` sempre
- ❌ `ExecSQL` com `RETURNING` — usar `Open`
- ❌ `PAGE_SIZE 4096` — usar 16384 para produção
- ❌ Ignorar deadlocks — tratar `ekRecordLocked`
- ❌ Queries sem índice em colunas de WHERE/JOIN
- ❌ `CREATE TABLE IF NOT EXISTS` — não existe no Firebird (verificar metadata)

## Metadata — Verificar Existência

```pascal
{ Tabela existe? }
SELECT COUNT(*) FROM RDB$RELATIONS
WHERE RDB$RELATION_NAME = 'CUSTOMERS' AND RDB$SYSTEM_FLAG = 0;

{ Coluna existe? }
SELECT COUNT(*) FROM RDB$RELATION_FIELDS
WHERE RDB$RELATION_NAME = 'CUSTOMERS' AND RDB$FIELD_NAME = 'EMAIL';

{ Generator existe? }
SELECT COUNT(*) FROM RDB$GENERATORS
WHERE RDB$GENERATOR_NAME = 'GEN_CUSTOMER_ID' AND RDB$SYSTEM_FLAG = 0;
```
